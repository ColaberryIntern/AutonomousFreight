node_modules/
dist/
coverage/
*.tsbuildinfo

# Env files — NEVER commit secrets
.env
.env.local
.env.*.local
config/development.env
config/staging.env
config/production.env

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# Autonomy runtime artifacts (content is dynamic; schemas are committed)
tmp/autonomy_log.json
tmp/escalation.json
!tmp/*.schema.json
!tmp/README.md

# RFQ regression corpus: real ShipCES customer emails, fetched read-only from
# Karun's repo via scripts/corpus/fetchCorpus.js. The payload must never enter
# our git history; the committed corpus-manifest.json (hashes + metadata) is
# what makes runs reproducible.
tests/fixtures/corpus/
