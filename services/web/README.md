# @af/web

Sprint 5 ships the API contract + smoke tests as the MVP backend gate.

The full Vite + React shell (login form + dashboard placeholder) lands in **Sprint 17 (UAT)** when we have a deployment story to host it against. Until then, all UI verification is via Swagger UI (`/docs` once Sprint 13 wires it) and direct API calls.

This package exists so the workspace declaration stays valid.
